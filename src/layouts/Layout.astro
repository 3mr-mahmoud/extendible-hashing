<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href="/extendible-hashing/favicon.ico" />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="preload" as="style" onload="this.onload=null;this.rel='stylesheet'">
		<meta name="generator" content={Astro.generator} />
		<title>Extendible Hash Visualizer</title>
		<meta name="google-site-verification" content="cYBbm8suAG9ZpJ4bT7CcdH8o0zrf4mjaXMqPwUT8Hqg" />
		<meta name="description" content="An interactive visualization tool for extendible hashing, a dynamic hashing technique used in database systems to efficiently manage and access large datasets." />
		<meta name="keywords" content="Extendible Hashing, Data Structures, Algorithms, Visualization, Database Systems, Hash Tables, Dynamic Hashing, Computer Science, Educational Tool" />
		<meta name="author" content="Amr Mahmoud" />
		<meta property="og:title" content="Extendible Hash Visualizer" />
		<meta property="og:description" content="An interactive visualization tool for extendible hashing, a dynamic hashing technique used in database systems to efficiently manage and access large datasets." />
		<meta property="og:type" content="website" />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content="Extendible Hash Visualizer" />
		<meta name="twitter:description" content="An interactive visualization tool for extendible hashing, a dynamic hashing technique used in database systems to efficiently manage and access large datasets." />

	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		min-height: 100%;
		background-color: #0a0a0a;
		color: #e5e5e5;
		font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
	}

	html {
		scrollbar-gutter: stable;
	}

	body {
		line-height: 1.6;
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
	}

	/* Remove default focus styles and add custom ones */
	*:focus {
		outline: none;
	}

	button:focus-visible,
	input:focus-visible,
	select:focus-visible,
	textarea:focus-visible {
		outline: 2px solid #667eea;
		outline-offset: 2px;
	}


	.main-title {
		text-align: center;
		font-size: 3rem;
		margin-top: 2rem;
		margin-bottom: 1rem;
		background: linear-gradient(90deg, #5227FF, #FF9FFC, #B19EEF);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		color: transparent;
	}

	.footer {
    position: relative;
    width: 90%;
    height: 390px;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    background: rgba(0, 0, 0, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-right: auto;
    margin-left: auto;
    border-radius: 4rem;
    margin-bottom: 3rem;
}
.footer h1 {
	font-size: 2.5rem;
	color: white;
	text-align: center;
	z-index: 9999;
	user-select: none;
	margin-bottom: 0;
	display: inline-block;
	pointer-events: none;
}

@media (max-width: 768px) {
	.footer h1 {
		font-size: 1.8rem;
	}
}
	.credits-bar {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 15px 30px;
		background: #111111;
		border-top: 1px solid #333;
		color: #888;
		font-size: 14px;
		margin-top: 20px;
	}

	.credits-left {
		display: flex;
		align-items: center;
		gap: 5px;
	}

	.credits-right {
		font-size: 13px;
		opacity: 0.8;
	}

	.credits-bar a {
		color: #fff;
		text-decoration: underline;
		transition: color 0.3s ease;
	}

	.heart {
		width: 16px;
		height: 16px;
		fill: #e74c3c;
		animation: heartbeat 1.5s ease-in-out infinite;
		display: inline-block;
		vertical-align: middle;
	}

	@keyframes heartbeat {
		0% {
			transform: scale(1);
		}
		14% {
			transform: scale(1.2);
		}
		28% {
			transform: scale(1);
		}
		42% {
			transform: scale(1.2);
		}
		70% {
			transform: scale(1);
		}
	}

	/* Responsive design for credits bar */
	@media (max-width: 768px) {
		.credits-bar {
			flex-direction: column;
			gap: 10px;
			text-align: center;
			padding: 20px 15px;
		}
		
		.credits-right {
			font-size: 12px;
		}
	}
</style>
